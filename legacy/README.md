# Legacy Code

This directory contains legacy Python code that is no longer part of the production runtime.

**Production runtime**: TypeScript/Node only. See `src/` and `package.json` scripts.

- `python/` — Former Python implementation (scheduler, orchestrator, workflows). Kept for reference; not executed in production.
